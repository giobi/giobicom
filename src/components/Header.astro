---
const navigation = [
  { name: 'Home', href: '/giobicom25/' },
  { name: 'Servizi', href: '/giobicom25/#servizi' },
  { name: 'Chi Siamo', href: '/giobicom25/#chi-siamo' },
  { name: 'Contatti', href: '/giobicom25/#contatti' },
];
---

<nav class="bg-black border-b border-green-400 sticky top-0 z-50 retro-border">
  <div class="container-custom">
    <div class="flex justify-between items-center py-4">
      <!-- Logo -->
      <div class="flex items-center space-x-3">
        <a href="/giobicom25/" class="flex items-center space-x-3 hover:opacity-80 transition-opacity">
          <div class="w-10 h-10 retro-border bg-black flex items-center justify-center">
            <span class="text-green-400 font-bold text-xl">{</span>
          </div>
          <div class="hidden sm:block">
            <div class="text-xl font-bold text-green-300">giobi.com</div>
            <div class="text-sm text-green-500">[ SVILUPPO WEB & LARAVEL ]</div>
          </div>
        </a>
      </div>
      
      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center space-x-8">
        {navigation.map((item) => (
          <a
            href={item.href}
            class="text-green-400 hover:text-green-300 font-bold transition-colors duration-200 uppercase tracking-wider"
          >
            {item.name}
          </a>
        ))}
        <a href="/giobicom25/#contatti" class="btn-primary">
          CONTATTACI
        </a>
      </div>
      
      <!-- Mobile menu button -->
      <div class="md:hidden">
        <button
          type="button"
          class="mobile-menu-button text-green-400 hover:text-green-300 focus:outline-none focus:text-green-300"
          aria-label="Toggle menu"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path
              class="menu-icon"
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16"
            />
            <path
              class="close-icon hidden"
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M6 18L18 6M6 6l12 12"
            />
          </svg>
        </button>
      </div>
    </div>
    
    <!-- Mobile Navigation -->
    <div class="mobile-menu hidden md:hidden pb-4">
      <div class="flex flex-col space-y-3">
        {navigation.map((item) => (
          <a
            href={item.href}
            class="text-green-400 hover:text-green-300 font-bold py-2 transition-colors duration-200 uppercase tracking-wider"
          >
            {item.name}
          </a>
        ))}
        <a href="/giobicom25/#contatti" class="btn-primary w-fit mt-4">
          CONTATTACI
        </a>
      </div>
    </div>
  </div>
</nav>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const mobileMenuButton = document.querySelector('.mobile-menu-button');
    const mobileMenu = document.querySelector('.mobile-menu');
    const menuIcon = document.querySelector('.menu-icon');
    const closeIcon = document.querySelector('.close-icon');
    
    if (mobileMenuButton && mobileMenu && menuIcon && closeIcon) {
      mobileMenuButton.addEventListener('click', () => {
        mobileMenu.classList.toggle('hidden');
        menuIcon.classList.toggle('hidden');
        closeIcon.classList.toggle('hidden');
      });
      
      // Close mobile menu when clicking on a link
      const mobileLinks = mobileMenu.querySelectorAll('a');
      mobileLinks.forEach(link => {
        link.addEventListener('click', () => {
          mobileMenu.classList.add('hidden');
          menuIcon.classList.remove('hidden');
          closeIcon.classList.add('hidden');
        });
      });
    }
  });
</script>